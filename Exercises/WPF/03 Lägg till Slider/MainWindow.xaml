<Window x:Class="_03_Lägg_till_Slider.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_03_Lägg_till_Slider"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
            Orientation="Horizontal" 
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <Button x:Name="DecreaseBTN" Content="Decrease" Padding="5" Click="DecreaseBTN_Click"/>
            <Label x:Name="CounterLBL" Content="5" Margin="15,0"/>
            <Button x:Name="IncreaseBTN" Content="Increase" Padding="5" Click="IncreaseBTN_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="1"
            Orientation="Horizontal" 
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <Slider x:Name="Slider" Value="{Binding Content, ElementName=CounterLBL}" Minimum="0" Maximum="25" Width="150" IsSnapToTickEnabled="True"/>
        </StackPanel>
    </Grid>
</Window>
